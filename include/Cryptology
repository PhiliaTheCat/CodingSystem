#ifndef __INCLUDE_CRYPTOLOGY__
    #define __INCLUDE_CRYPTOLOGY__

    // String Processing
    inline void Remove_Invalid(char key[]);
    inline void Capitalize(char key[]);
    inline void Remove_Replication(char key[]);

    // Confirmation
    inline bool Deny_Str(char str[], int branch);

    // Key Completion
    inline void Complete_Key(char key[]);
#endif
